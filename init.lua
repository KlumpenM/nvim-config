-- Require the config for remapping keys
require('config.remap')
-- Require the settings for customizing neovim
require('config.set')

-- Require the setting up for lazy init, so that we can have a folder for all the plugins that we
-- are using
require('config.lazy_init')
